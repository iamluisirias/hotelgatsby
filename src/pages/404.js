import React from 'react';
import Layout from '../components/layout';

const NotFoundPage = () => {
    return (
        <Layout>
            <h1>NOT FOUND</h1>
        </Layout>
    );
};

export default NotFoundPage;